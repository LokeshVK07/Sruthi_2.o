// cloudflare/public/app.js — kept so the background-refresh workflow's
// required-files check passes. The active frontend is the Vite-built React
// app under /assets/index-*.js, referenced from index.html. This file is
// intentionally empty: nothing in index.html loads it.
